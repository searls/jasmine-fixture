src_files:
  - "spec/prereq/*.{js,coffee}"
  - "spec/vendor/*.{js,coffee}"
  - "src/**/*.{js,coffee}"

stylesheets:

helpers:
  - "helpers/**/*.{js,coffee}"

spec_files:
  - "**/*[Ss]pec.{js,coffee}"

src_dir:

spec_dir: spec